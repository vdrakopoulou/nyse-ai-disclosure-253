# TODO for a Fuller Archival Replication

1. Add the full raw document corpus for each source URL, not only one retained sentence per firm.
2. Add the primary filing-audit workbook so filing-specific intercoder reliability can be estimated directly.
3. Add an adjudication log or decision rules so coder disagreements can be resolved reproducibly.
4. Add the formal codebook for AI terms, library terms, OCI rules, and exclusion rules if a separate version exists.
5. Add the larger frozen sampling frame if the intended study universe is 546 firms rather than the 253 included issuers visible here.
6. Add any omitted sectoral press dataset if that channel was part of the original design.
7. Add example paper tables or figure callouts if exact publication formatting must be reproduced.
